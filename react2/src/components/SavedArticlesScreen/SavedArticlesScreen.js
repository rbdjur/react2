import React from "react";


const SavedArticlesScreen = () =>
    <div>
        Saved Articles go here + button to remove the saved Articles
        - research and refresh memory on axios because this will need to call to the Mongodata base and use a delete command.
    </div>


export default SavedArticlesScreen;